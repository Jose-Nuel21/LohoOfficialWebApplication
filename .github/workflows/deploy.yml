name: Deploy Site on push

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout latest code
      uses: actions/checkout@v2

    - name: 📂 Sync files via FTPS
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ${{ secrets.LARAVEL_SITE_SERVER }}
        username: ${{ secrets.LARAVEL_SITE_USER }}
        password: ${{ secrets.LARAVEL_SITE_PASS }}
        server-dir: /public_html/loho/  # This is the FTPS-visible path
        local-dir: ./                   # Syncs the root Laravel project
        protocol: ftps
        port: 21
        dangerous-clean-slate: false
        verbose: true

    - name: 🛠 Install Dependencies, Run Migration and Optimize
      uses: garygrossgarten/github-action-ssh@release
      with:
        command: |
          cd /home2/grandslo/public_html/loho
          composer install --no-dev --optimize-autoloader
          php artisan migrate --force
          php artisan optimize:clear
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        privateKey: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SSH_PORT }}

    - name: 🧹 Post-deployment tasks
      uses: garygrossgarten/github-action-ssh@release
      with:
        command: |
          cd /home2/grandslo/public_html/loho
          php artisan config:cache
          php artisan route:cache
          php artisan view:cache
          chmod -R 775 storage bootstrap/cache
          chmod 644 .env
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USERNAME }}
        privateKey: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.SSH_PORT }}
